// classes/Criterio.js

class Criterio {
  constructor(id_criterio, id_curso, criterio, valor) {
    this.id_criterio = id_criterio;
    this.id_curso = id_curso;
    this.criterio = criterio;
    this.valor = valor;
  }
}

export default Criterio;
